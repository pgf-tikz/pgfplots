\documentclass{article}

\usepackage{pgfplots}
\pgfplotsset{compat=1.6}

\usepgfplotslibrary{patchplots}


\begin{document}

\begin{tikzpicture}
%\tracingcommands=2\tracingmacros=2
 \begin{axis}
  \addplot3[surf,
            fill=white,
            samples=10,
            samples y=10, %72,
            domain=-1:1,
            y domain=0:360,
			restrict expr to domain={x + y}{0:99999},
	%patch type=bicubic, patch type sampling, samples y=10, shader=faceted interp,
            z buffer=sort]
            ( {cosh(x)*cos(y)}, {cosh(x)*sin(y)}, {sinh(x)} );
 \end{axis}
\end{tikzpicture}

\end{document}
