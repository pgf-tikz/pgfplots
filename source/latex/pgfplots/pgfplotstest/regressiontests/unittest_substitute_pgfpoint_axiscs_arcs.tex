\documentclass{standalone}
\usepackage{pgfplots}
	\pgfplotsset{compat=1.11}

\begin{document}

\begin{tikzpicture}[>=latex]
		\begin{axis}
		[
			title={1.11},
			xmin=-1.5,
			xmax=1.5,
			xtick = {-1,...,1},
			ymin=-1.5,
			ymax=1.5,
			ytick = {-1,...,1},
			grid = both,
			axis x line=middle,
			axis y line=middle,
		]
			
		\draw[red,thick,->] (axis cs:1,0)
			arc[radius={transformdirectionx(1)}, start angle= 0, end angle=-90];

		\draw[green,thick,->] (axis cs:0,-1)
			arc[radius={1}, start angle= -90, end angle=-180];

		\draw[blue,thick,->] (0,0)
			arc[radius={1cm}, start angle= 0, end angle=45];
		
		\end{axis}
\end{tikzpicture}

\begin{tikzpicture}[>=latex]
		\begin{axis}
		[
			compat=1.10,
			title={1.10},
			xmin=-1.5,
			xmax=1.5,
			xtick = {-1,...,1},
			ymin=-1.5,
			ymax=1.5,
			ytick = {-1,...,1},
			grid = both,
			axis x line=middle,
			axis y line=middle,
		]
			
		\draw[red,thick,->] (axis cs:1,0)
			arc[radius={transformdirectionx(1)}, start angle= 0, end angle=-90];
		
		\draw[green,thick,->] (axis cs:0,-1)
			arc[radius={transformdirectionx(1)}, start angle= -90, end angle=-180];

		\draw[blue,thick,->] (axis cs:0,0)
			arc[radius={1cm}, start angle= 0, end angle=45];
		\end{axis}
\end{tikzpicture}

\end{document}
