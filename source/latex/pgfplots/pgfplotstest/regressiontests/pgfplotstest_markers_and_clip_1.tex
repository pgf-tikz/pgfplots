\documentclass[a4paper]{article}

\usepackage{pgfplots}

\pgfplotsset{compat=1.5}
\usepackage{pgfplotstest_markers_and_clip}

\begin{document}

\TEST{
	clip mode=individual,
	title=blue mark below red line not clipped,
}

\TEST{
	clip mode=individual,
	set layers,
	mark layer=axis foreground,
	title=blue mark above red line (mark layer) not clipped,
}
\end{document}

