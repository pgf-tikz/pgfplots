copylines = 0; nblocks = 25; nlines = 25; if copylines == nil or nblocks == nil or nlines == nil then io.stderr:write("failed to parse arguments"); os.exit(2); end; yvaries = 'x'; yvaries = (yvaries == 'y'); infile = io.open("example_286_contourtmp0.dat", "r"); outfile = io.open("example_286_contourtmp0.table", "w"); mesh = PrepcMesh.new(yvaries, nblocks, nlines, copylines, infile, outfile); corners = false; levels = {1}; for n = 1, #levels do level = tonumber(levels[n]); if level then mesh:contour(level, corners); end; end; io.close(infile); mesh:printcontours(); io.close(outfile);
